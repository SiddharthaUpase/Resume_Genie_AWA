
const apiUrl = "https://flask-hello-world-two-dusky.vercel.app";
const localUrl = "http://127.0.0.1:8000";

const currentApiUrl = apiUrl;

const urls = {
    apiUrl,
    localUrl
}

export default currentApiUrl;